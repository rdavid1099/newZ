<ul>
  <% current_user.stations.each do |station| %>
    <ul><strong><%= link_to station.name, station_path(station.call_letters) %></strong><br>
      Subscribers: <%= station.num_of_subscribers %></ul>
  <% end %>
</ul>
